import Ourcollection from '@/components/home/collection'
import Hero from '@/components/home/Hero'
import Services from '@/components/home/services'
import TopCollection from '@/components/home/topCollection'
import React from 'react'

const page = () => {
  return (
    <div>
      {/* <Hero/>  it will be of a collection of my choosing i will add , a video or an image then some overlay text and a link that leads to its page */}
      {/* then below it the shortcuts to my brand abayas , handbags , fragrances ,acceories  */}
      {/* then Summer collection from seasonal collection header , below it image or video explaining this collection or repping it , then 4 itema sthat belng to said collection then the cta below to dicover more */}
      {/* then Vn Models ,  */}
      {/* then lux infinty , heade text , then intor image or vidoe , then 4 items, then cta */}
      {/* then 4 best sellers , cta */}
      {/* then Vn services below  */}
      <Services/>
    </div>
  )
}

export default page
